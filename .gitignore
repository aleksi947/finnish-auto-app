# ───── Logs ─────
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
firebase-debug.log
firestore-debug.log
ui-debug.log
pglite-debug.log

# ───── Dependencies ─────
node_modules

# ───── Build output ─────
dist
dist-ssr

# ───── Environment configs ─────
.env
.env.local
.env.*.local
.env.development
.env.production

# ───── Firebase secrets ─────
serviceAccountKey.json

# ───── Functions ─────
# node_modules и .env уже игнорируются общими правилами выше, 
# но можно оставить для явности или если .env не в корне
functions/.env

# ───── Upload Lessons ─────
# Игнорируем секреты и зависимости, но оставляем скрипты
upload-lessons/serviceAccountKey.json
upload-lessons/node_modules
upload-lessons/.env
# Скрываем контент уроков (JSON), чтобы не попал в git
upload-lessons/lessons/
upload-lessons/lessons.json

# ───── Editor config files ─────
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.cursor/

# ───── Misc ─────
*.local
